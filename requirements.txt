# --- Web framework ---------------------------------------------------------
Flask==3.0.3
Jinja2==3.1.4
Werkzeug==3.0.3
python-dotenv==1.0.1

# --- Testing ------------------------------------------------------------------
pytest==8.2.2
pytest-html==4.1.1
pytest-json-report==1.5.0

# --- Selenium -----------------------------------------------------------------
selenium==4.22.0
webdriver-manager==4.0.2

# --- Reporting ------------------------------------------------------------------
reportlab==4.2.2      # PDF report generation
Jinja2==3.1.4          # HTML report templating (duplicate pin kept intentional)

# --- AI integration -----------------------------------------------------------
google-generativeai>=0.7.0  # Official Gemini SDK (supports AQ auth keys)
requests==2.32.3             # HTTP client (kept for other uses)

# --- Utilities -----------------------------------------------------------------
python-dateutil==2.9.0
